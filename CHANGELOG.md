# 更新记录

本项目采用语义化版本号。面向使用者的完整公告发布在 GitHub Releases；本文件用于在仓库中长期记录每个版本的主要变化。

## [未发布]

## [3.0.1] - 2026-09-02

### 修复

- 为内置 `mabowx` 显式声明 Windows 必需的 `pywin32`，修复全新安装后微信 Bot 因缺少 `win32api` 持续重启的问题。
- 安装器与启动引导现在会检查全部实际使用的 Win32 模块；依赖缺失时会自动重新执行环境安装，不再把只能导入顶层 `mabowx` 的环境误判为就绪。
- `mabowx` 环境自检补充窗口、进程、互斥锁、安全令牌与截图相关的 pywin32 模块检查。

## [3.0.0] - 2026-09-02

### 重大变更

- 微信 UI 控制层从旧自动化库完整迁移到项目内置的 `mabowx`；源码、启动器、配置命名空间、文件工具和运行目录统一使用 Mabobot 命名。
- 删除旧兼容层和历史恢复路径，备份格式固定为 `mabobot-mabowx-backup` v2；升级后必须完整重启。
- 主程序与 `mabowx` 建立明确边界：UI 操作、监听、发送和窗口修复由 `mabowx` 负责，应用层只承担业务编排、幂等、权限和持久化。

### 新增

- 新增与 Web 控制台同一视觉体系的独立桌面启动器，提供服务监管、实时日志、环境检查、系统托盘和异常退避恢复。
- 根目录统一为 `START.bat` 单一用户入口；首次运行在轻量引导窗口中自动准备依赖。
- 微信入站文件会即时归档并建立持久索引；用户引用历史文件名时可精确绑定原文件，随后交给 Codex 读取、润色、总结或生成新产物。
- Codex 运行中心支持删除持久会话，任务时间线展示安全裁剪后的命令、文件变更、工具调用、搜索和生成物进度，并标明来源聊天。

### 安全

- Codex 聊天新增持久的访问策略：群聊与普通私聊默认使用按聊天划分的隔离目录，隔离进程不继承本机用户配置或命令放行规则；仅管理员显式指定的私聊保留本机最大权限和持久会话。
- 微信附件会先复制进当前聊天的请求目录再交给 Codex，生成文件也保留在同一聊天范围内；群成员共享群目录，但不同聊天之间不可互读。

### 界面优化

- 聊天管理详情新增紧凑的“Codex 访问范围”控件，可查看实际目录并为私聊授予或撤销管理员权限；群聊固定显示共享隔离状态。

### 变更

- “随 Windows 登录启动”和“启动前自动确认微信登录”由桌面面板直接管理；既有登录启动快捷方式会在首次打开时自动迁移。
- 产品名称、管理界面、启动器、文档和公开仓库统一使用 Mabobot；新建模型凭据只使用 `MABOBOT_MODEL_*` 命名空间。
- 备份硬切换为 `mabobot-mabowx-backup` v2；所有旧代际归档均不再列出、导入或恢复。

### 修复

- 完整迁移现在强制包含内置 `mabowx/`、选择器和新版下载目录；状态备份纳入 Codex 聊天空间、微信附件及可迁移文件索引，并阻止状态恢复覆盖依赖清单。
- 图片任务在模型返回损坏或缺失的附件时，会校验并恢复同一线程保存的有效原图后再发送；不限制用户明确要求的生成数量。
- 群昵称自动校准发生变化时保留旧昵称作为触发别名，避免群成员继续使用旧 `@` 名称时机器人失联。
- 管理员私聊改用稳定 App Server 的 `danger-full-access` 沙箱字段，避免实验权限参数导致文件任务在线程创建前失败。

### 可观测性

- 监听窗口 Win32 看护复用既有 5 秒几何扫描，仅在窗口语义状态、可见性、最小化状态或 HWND/PID 变化时写入持久审计；每条变化同时记录上一轮实际观察时间，并在监听状态接口中保留有界历史。

## [2.2.3] - 2026-08-25

### 新增

- 群聊文本发送支持精确、可验证的真实 `@` 对象；发送前校验候选成员和富文本对象，避免把普通文本误当成提醒。
- 备份管理新增永久删除操作，并保护正在等待离线恢复的归档不被删除。
- 完整迁移可按配置包含 WSL Codex 中的 PPT Master 技能，并在恢复时校验目标边界。

### 可靠性

- 独立聊天窗口统一改用轻量 Win32 几何监测：每 5 秒检查一次，同一 HWND 连续两次落入 `-32000` 无效位置后自动原位复位。
- 删除旧的监听窗口周期巡检、主会话缺口推断、UIA 探针、强制重建、消息补扫、凌晨重置及其启用开关；新监测随微信桥接固定启动，不激活、不关闭也不重建窗口。
- 主应用发现监听窗口真实缺失时，通过正常监听入口逐个恢复，并对单个聊天独立退避。

### 界面优化

- 备份与迁移页面收口为紧凑操作栏，补充待恢复标记、删除入口和移动端布局。

## [2.2.2] - 2026-08-24

### 新增

- 普通聊天新增内置 DDGS 网页搜索和受限网页正文读取，不再需要单独配置搜索模型或搜索 API Key。
- 模型管理页面新增 LiteLLM 版本检查、原环境内更新、独立进程校验和失败自动回滚。

### 变更

- 搜索规划、图片输入和最终回复统一复用 `builtin_chatbot.chat` 路由；Codex 继续使用其原生搜索能力。
- 引用图片严格绑定本轮消息：视觉模型直接读取图片，非视觉模型使用聊天记录插件生成的文字补充，不再回退到无关历史图片。
- 人物记忆统一为单一证据账本和投影链路，移除旧人物摘要与重复写入实现；遗留表保留但运行时不再读写。
- 群聊无 `@` 续聊改为受控对话行为分类，可识别回答、结果反馈、追问、纠正、澄清和实质补充，并允许话题交错。

### 可靠性

- 监听器会话缺口需要跨巡检确认，并可由迟到回调或出站活动取消；同一会话记录不会反复触发破坏性重建。
- 修正监听会话时间对宿主机时区的解释，降低正常消息被误判为监听缺失的概率。
- Weekly 在提交托管任务前原子登记调度，并使用跨热重载保留的机器绑定锁，避免重复运行。
- 修复图片理解模型降级、流式补救内部参数泄漏、域名黑名单解析和已识别微信消息类型归一化问题。

## [2.2.1] - 2026-08-21

### 可靠性

- 微信桥接健康检查改为常数时间的缓存快照，不再由轮询请求并发调用微信 UI 自动化接口。
- 微信在线探针改为单一所有者，并为失败和恢复增加连续样本确认；UI 锁繁忙时保留最近确认状态，不再误判掉线。
- 主应用区分桥接超时、无效响应和微信真实离线，并绕过本机桥接请求的系统代理设置。
- 新增桥接进程实例、微信连接代次和在线探针诊断字段；只有连接实例真正变化时才重绑监听器。
- 新增纯进程存活端点 `/live`，便于长期无人值守设备通过 SSH 或外部监控分层排障。

### 修复

- 修复消息发送、媒体处理或监听器巡检占用微信 UI 时，连续健康轮询可能错误标记连接不可用并触发无效恢复的问题。
- 修复短暂 HTTP 超时恢复后被当成微信重新连接，进而重复同步监听器的问题。

## [2.2.0] - 2026-08-21

### 新增

- 新增系统级备份、校验与恢复流程，提供“状态备份”和“完整迁移”两种类型，并在恢复前生成安全备份。
- 新增统一运行状态中心，集中展示后台操作、插件健康、托管 worker、存储占用及审计记录。
- 新增 Codex 运行环境检测、一键升级、版本结果与升级过程展示。
- 概览页新增系统健康、插件健康、后台任务、Mabobot 运行时长和本机开机时长。

### 变更

- 仓库中的全部内置插件均已接入统一管理；不再加载缺少版本声明或仍使用旧接入方式的插件。
- 插件后台任务、调度 worker、健康检查、清理钩子和数据目录统一归属平台运行时；后续插件直接遵循同一接口接入。
- 插件数据按 `persistent`、`generated`、`cache`、`machine_bound`、`temporary` 分类管理，旧目录在首次启动时自动迁移。
- Summary Plus 保留现有能力并完成模块化拆分，将浏览器、媒体管线、平台服务、任务准入与小红书处理从入口文件中解耦。
- Codex 会话改为统一运行时与有界并发管理，可随全局 Codex 环境升级，并加强超时、取消和进程回收。
- 模型连接与路由配置进一步统一，补充 Gemini 新版本的参数约束与诊断信息。

### 界面优化

- 重排 Codex 管理区，移除占空间过多、割裂感明显的卡片布局。
- 运行状态各区块默认折叠，并显示简短摘要，避免页面纵向无限铺开。
- 运行状态按“插件状态 → 后台任务 → 存储空间 → 最近问题 → 变更记录”重排；“高级设置”改为末尾的“扩展设置”。
- 存储扫描改为局部更新进度，消除扫描期间整页闪动。
- 备份与迁移页面改为更克制的摘要栏、配置区和结果区布局。

### 修复

- 修复插件卸载时后台任务和线程可能继续存活的问题。
- 修复 Weekly、JR 库存和中行汇率定时任务绕过统一后台操作记录的问题。
- 修复多个插件产物、缓存和状态文件散落在插件目录或公共数据目录的问题。

### 升级提醒

- 这是插件接入方式的破坏性升级。自定义插件必须声明 `plugin_api_version: 2`，并实现 `register(event_bus, subscribe, context)`。
- 更新代码后必须重启 Mabobot，首次启动会执行插件数据目录迁移。
- 当前备份格式会包含明文 `.env`，归档尚未加密；请仅保存在可信本机或加密磁盘中。

[3.0.1]: docs/releases/v3.0.1.md
[3.0.0]: docs/releases/v3.0.0.md
[2.2.3]: docs/releases/v2.2.3.md
[2.2.2]: docs/releases/v2.2.2.md
[2.2.1]: docs/releases/v2.2.1.md
[2.2.0]: docs/releases/v2.2.0.md
