# 公开版拆分说明

这个目录是从私有工作区当前版本按白名单整理的独立审查副本，不包含原仓库的 Git 历史。

公开版默认使用 Web 端口 `8888` 和微信桥接端口 `5555`，两者均可在首次启动生成的 `.env` 中调整。

管理面板品牌已与主项目统一为 `GGBot`。任务路由会根据保留插件的 `ui.llm_tasks` 声明显示空白待配置任务，不再因公开版未预置模型而显示空页。模型表单也会在保存前清理粘贴带入的零宽字符和全角符号，并把原先多种凭据保存选项合并为一个 API Key 输入框；每个模型使用独立本机凭据，模型配置不写入明文。

本次已重新同步私有工作树的最新代码，包括微信文本发送去重与受控重试、发送成功后再写入 AI 上下文、模型连接/本地凭据管理、系统状态以及 Web 日志查找体验；运行数据和私人品牌仍未带入。

## 保留的插件

- `builtin_chat_logger`：聊天记录
- `builtin_translation`：翻译助手
- `boc_rate`：汇率查询
- `Weekly`：周报助手
- `magnet_check`：磁链检查
- `menu_translator`：菜单翻译
- `builtin_chatbot`：AI 助手完整功能

## 已执行的脱敏

- 未复制 `.env`、数据库、模型文件、聊天记录、记忆库、日志、缓存、Cookie 和生成报告。
- 所有插件 `config.json` 均恢复为通用 schema 默认值。
- 删除个人角色、Judge、翻译、菜单、周报署名、管理员和邮箱默认值。
- 模型列表和任务映射首次启动均为空，用户必须自行添加。
- AI 长期记忆与后台记忆默认关闭，但相关代码和管理界面完整保留。
- 路由表只包含上述七个插件。

## 审查重点

公开前仍建议人工确认：项目名称、许可证、README 中的安装要求、`wxautox4` 版本，以及 Weekly 对本地 Codex CLI 的依赖是否符合预期。
